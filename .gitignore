# Build artifacts
build/
dist/
*.exe
# NOTE (W2-004): the PyInstaller recipe is tracked so releases are reproducible from a known commit.
# *.spec is intentionally NOT ignored.

# Python
__pycache__/
*.pyc
*.backup
*.bak

# Logs
deleted_log.txt

# SAIPEN local runtime state (per-machine, never project content)
.saipen/recovery/
.saipen/locks/
.saipen/cache/
